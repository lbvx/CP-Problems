#include <bits/stdc++.h>

using namespace std;

int main()
{
    int B, D;
    cin >> B >> D;
    string num;
    cin >> num;

    string comp2 = num;

    int i = num.size() - 1;
    for (int i = num.size() - 1; i >= 0; i--)
    {
        if (num[i] == '1')
        {
            
        }
    }
    // cout << num << endl;

    return 0;
}